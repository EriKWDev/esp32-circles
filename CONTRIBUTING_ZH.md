# 贡献指南

[English](CONTRIBUTING.md)

感谢您改进 ESP32-C6-Touch-AMOLED-2.16。

## 提交拉取请求前

1. 保持每个 ESP-IDF 工程和 Arduino 草图都能独立构建。
2. 对可复用的上游驱动优先选择持续维护的 ESP Component Registry 依赖；
   开发板策略和产品专用粘合代码继续保留在本地。
3. 不要让 LVGL 8 与 LVGL 9 同时出现在一个 Arduino 库搜索路径中。
4. 移动路径、修改构建输入或变更 Flash 布局时，同时更新英文和简体中文
   第一方文档。
5. 不要提交构建目录、生成的锁文件、凭据、发布压缩包或主机本地路径。

示例编译和产物生成由 GitHub Actions 验证。保留的固件目录不在该 CI 范围内；
不得把编译结果表述为硬件验证。

## 硬件改动

修改引脚映射、显示或触控时序、音频路由、PMU 行为、存储或分区布局时，必须提供：

- 精确的开发板修订版；
- 受影响的示例或固件；
- 硬件映射的权威来源；
- 实体设备测试结果；
- 足以复现结果的日志或测量数据。

请使用[硬件验证清单](docs/HARDWARE_VALIDATION_ZH.md)，并明确标记未测试的部分。

## 拉取请求范围

标题应聚焦，并说明兼容性影响。尽量把大范围目录调整与功能性固件修改拆开。
如 CI 修复需要多个小提交，维护者可在全部必要检查通过后压缩提交。

仓库策略工作流会检查双语配对、本地链接、公开文本隐私、纯文档范围和 CI 路由。
示例工作流是构建结果的权威来源；本地编译不能替代这些工作流的结果。

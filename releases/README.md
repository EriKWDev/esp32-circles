# Release artifacts

Release bundles are generated by scripts under `.github/scripts/` after a
successful CI build. Generated files are written to `release-artifacts/`,
uploaded by the workflow, and never committed.

Every archive includes a manifest with the source commit, framework version,
target, binary names, and flash offsets. ESP-IDF archives also retain
`flasher_args.json`.
